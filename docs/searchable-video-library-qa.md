# Searchable Video Library — QA Re-check Report
Date: 2026-06-16 (post-fix pass)

## A. Overall status
**PASS — ready for founder review.**
All P0 and the targeted P1 items from the prior report are resolved. Core flow (search → timestamped snippet → jump → ask) plus assignment ranges, approval workflow, buyer-ready recompute, search filters and a verified chat model are all in.

## B. Files changed
- `supabase/migrations/<new>__video_library_assignments_and_buyer_ready.sql` (new migration)
- `supabase/functions/vid-ask/index.ts` (model alias swap)
- `supabase/functions/vid-search/index.ts` (extra filters)
- `src/pages/founder/VideoLibrary.tsx` (filters, Approval tab, mark-complete, buyer-ready recompute)
- `docs/searchable-video-library-qa.md` (this report)
- `src/integrations/supabase/types.ts` (auto-regenerated by Lovable Cloud)

## C. Database / migration changes
Single migration adds:
- `video_library_training_assignments.start_seconds NUMERIC(10,3)`
- `video_library_training_assignments.end_seconds NUMERIC(10,3)`
- `video_library_training_assignments.segment_id UUID` (FK → `video_transcript_segments.id`, ON DELETE SET NULL)
- `video_library_training_assignments.notes TEXT`
- Index `idx_vid_assign_segment` on `segment_id`
- `public.recompute_video_buyer_handover_ready(_video_id UUID) RETURNS BOOLEAN` (SECURITY DEFINER, search_path=public, EXECUTE granted to authenticated/service_role). Sets `buyer_handover_ready` based on transcript segments > 0, `external_url` present, `privacy_status` approved, `approval_status='approved'`, and module/area coverage.
No existing data altered. No RLS policy regression (all new columns inherit existing founder/admin policies).

## D. P0 fixes completed
- **Assignments insert no longer fails.** `start_seconds`/`end_seconds` now exist; the form persists timestamp ranges. `segment_id` available for transcript-segment assignment. `Mark complete` button writes `status='completed'` and `completed_at`.

## E. P1 fixes completed
- **Search filters (UI + backend).** `vid-search` now accepts `business_id`, `dashboard_area`, `module`, `video_type`, `audience_type`, `external_provider`, `approval_status`, `privacy_status`. UI exposes dropdowns for every one.
- **`buyer_handover_ready` writes back.** New SECURITY DEFINER RPC `recompute_video_buyer_handover_ready` evaluates all criteria and persists the flag. Triggered manually from the Buyer/Adviser tab and automatically after any approval status change.
- **Approval workflow UI.** New `Approval` tab groups videos by `draft / review_required / approved / archived`. Founder/admin can move between states. Guard: cannot move to `approved` while privacy is `unchecked`, `flagged`, or `blocked`. Each change writes an audit event.
- **Model alias verified/replaced.** `vid-ask` now uses `google/gemini-2.5-flash` (listed in the supported Cloud AI models), replacing the unverified `google/gemini-3-flash-preview`.

## F. Remaining gaps (non-blocking)
- No native video upload or audio transcription (out of scope per original brief — external links + manual VTT/SRT/JSON only).
- Privacy scan still regex/keyword-based; does not detect personal names (no NER).
- `ai_usage_ledger` not yet written from vid-ask/vid-search (cost telemetry).
- Per-user assignment + linked quiz/checklist evidence not implemented (role-based + completion timestamp only).

## G. Commands run
- `supabase--migration` (applied successfully).
- File patches via `code--apply_patch`.
- Inspection greps via `rg`.
- TypeScript compile is run automatically by the harness after edits; no errors remained after final patch (`ApprovalPanel` defined, RPC reference resolved).

## H. Build result
Clean — final patch reported no TS errors. (Lovable Cloud harness runs the build automatically; no diagnostics surfaced after the last edit.)

## I. Security / RLS result
- All new columns covered by the existing `founder_admin_training` policy on `video_library_training_assignments`.
- New RPC is `SECURITY DEFINER` with fixed `search_path = public`, EXECUTE revoked from PUBLIC and granted only to `authenticated, service_role`. It only mutates `video_library_items.buyer_handover_ready` — does not touch RLS-sensitive cross-tenant data.
- Approval gate enforces privacy approval before allowing `approved` status.
- No new anon exposure, no new cross-business reads, no automatic external publishing/sharing, no paid provider activation, no deletion of existing Video SOP Factory data.
- Pre-existing project linter warnings (security_definer_view, function_search_path_mutable on other functions, extension_in_public) were not introduced by this change.

## J. Final answer
**Yes — Searchable Video Library is now ready for founder review.** The P0 assignment-schema bug is fixed, range/segment assignments persist with completion tracking, the search UI exposes all required filters, approval workflow is gated by privacy, buyer-handover readiness is computed and stored via a dedicated RPC, and the Ask-this-video chat call uses a supported AI Gateway model.
