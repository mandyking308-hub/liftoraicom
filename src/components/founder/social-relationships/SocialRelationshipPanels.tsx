// Backwards-compatible export. The production Founder page uses the safe
// implementation directly; this file remains only so any older imports resolve
// to the same confirmation-gated controls.
export * from "./SocialRelationshipPanelsSafe";
