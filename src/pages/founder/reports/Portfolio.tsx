import ReportList from "./ReportList";
export default function ReportsPortfolio() {
  return <ReportList title="Portfolio report" subtitle="Cross-venture portfolio view: scale / keep / watch / pause / retire recommendations and capital allocation cues." types={["portfolio"]} />;
}