import WorkloadView from "./WorkloadView";
export default function CapacityAgents() {
  return <WorkloadView title="AI agent workload" subtitle="Workload assigned to AI agents across all businesses." filterAssignee="ai_agent" />;
}