import { TypePage } from "./TypePage";
export default function RAAIBudget() {
  return <TypePage type="ai_budget" title="AI Budget Allocation"
    subtitle="AI Gateway spend across businesses. Internal allocation runs live; activating paid models or changing plans requires approval." />;
}